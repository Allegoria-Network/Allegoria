'use strict';
class ApiManager {
    constructor(client) {
        this._client = client;
    }

    async launch(id, data) {
        if (this.client.shard.ids[0] == 0) {
            cos
        }
    }
}

module.exports = ApiManager;